[build]
  command = "npm install --save-dev @rollup/rollup-linux-x64-gnu && npm ci && npm run build"
  publish = "dist"

# Handle client-side routing for single-page applications
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
